{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml"
        xmlns:image="http://www.google.com/schemas/sitemap-image/1.1">
  {{ $portrait := "img/hrishikesh_bhaskaran.jpg" | absURL }}
  {{ $portraitCaption := "Hrishikesh Bhaskaran (stultus) — Vice President of Engineering at Entri, screenwriter of Neelavelicham, FOSS contributor based in Kochi, Kerala" }}
  {{ $portraitTitle := "Hrishikesh Bhaskaran" }}
  {{ $ogImage := "img/hrishikesh_bhaskaran-bnw.png" | absURL }}
  {{ range .Data.Pages }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{ if not .Lastmod.IsZero }}<lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{ end }}
    {{ with .Sitemap.ChangeFreq }}<changefreq>{{ . }}</changefreq>{{ end }}
    {{ if ge .Sitemap.Priority 0.0 }}<priority>{{ .Sitemap.Priority }}</priority>{{ end }}
    {{ if eq .RelPermalink "/about/" }}
    <image:image>
      <image:loc>{{ $portrait }}</image:loc>
      <image:title>{{ $portraitTitle }}</image:title>
      <image:caption>{{ $portraitCaption }}</image:caption>
    </image:image>
    {{ else if eq .RelPermalink "/" }}
    <image:image>
      <image:loc>{{ $portrait }}</image:loc>
      <image:title>{{ $portraitTitle }}</image:title>
      <image:caption>{{ $portraitCaption }}</image:caption>
    </image:image>
    <image:image>
      <image:loc>{{ $ogImage }}</image:loc>
      <image:title>{{ $portraitTitle }}</image:title>
      <image:caption>{{ $portraitCaption }}</image:caption>
    </image:image>
    {{ end }}
  </url>
  {{ end }}
</urlset>
